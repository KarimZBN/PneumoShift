# Dados (grandes / externos — ver README para download e conversão)
dados/

# Resultados de execução: NÃO versiona os arquivos (CSVs, figuras — ficam local/nuvem).
# A estrutura de pastas é preservada no repositório por arquivos .gitkeep, adicionados
# manualmente com: git add -f resultados/**/.gitkeep
resultados/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Arquivos temporários do Office
~$*

# Editores / SO
.vscode/
.idea/
.DS_Store
Thumbs.db
